<li>
<p> <%= link_to contest.name, contest_path(contest) %> <p>
<% if logged_in? and owner?(contest) %>
    <%= link_to "delete" , contest_path(contest) , :method => :delete , data: {confirm: "Are you sure?"}%>  
<% end %>
<p> Type <%= contest.contest_type %>
<p> Contest description <%= contest.description %> </p>
<p> Deadline <%= contest.deadline %> </p>
<p> Start <%= contest.start %> </p>
<p> Referee <%= link_to contest.referee.name , referee_path(contest.referee) %> </p>
<p> Owner<%= link_to contest.user.username , user_path(contest.user) %> </p>
   
</li>